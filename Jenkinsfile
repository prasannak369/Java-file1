pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
				echo "build is done"
             }
		}
		stage('Test') {
            steps {
				echo "Test is done"
             }
		}
		 stage('Deploy') {
            steps {
				echo "Deploy is done"
             }
		}
	}
}
